# workflow-studio: island & UX — per-sheet cols, useSigned, handshake, per-scene sheets (Studio port follow-ups, #425)

Split from #425 (review residue of #424, the Studio port; epic #359) — the **island & UX** theme. Citations are against `origin/main` at `bd7e005`; paths under `apps/workflow-studio/` unless stated.

- [x] → #470 **Derive `columns` from CE's per-sheet `cols`.** `islands/cut-editor/filmstrip.ts:71` infers `cols = min(SHEET_COLS, max(1, sheet.times.length))` (`SHEET_COLS = 3` at `:50`). CE's `frames`+`tile` op (CE #706, v0.4.40 — the generic contact-sheet refactor) already returns `sheets[].cols` per sheet (`ffmpeg.handler.ts:1032`; min(chunk, tile.columns), narrower on a short final sheet) — no CE change needed. The rule's `check` step discards it: `rules/video/contact-sheet/post/check.fn.js:36` hardcodes `columns: 3` in the result `{ paths, times, columns, drawn }`. Carry `cols` per sheet through the result and read it in the island. Existing geometry tests in `filmstrip.test.ts` cover the short-final-sheet case.
- [x] → #471 **`useSigned` resolves all-or-nothing.** `islands/lib/useSigned.ts:110-141` signs via `Promise.all`, so the clip waits for every sheet. Only `cut-editor` is affected now (`cut-editor/App.tsx:255`; `blog-editor` uses its own on-demand cache per #433). Resolve per sheet so the first one renders as soon as it is signed.
- [ ] **`main.tsx` reports a handshake failure into the shell only** — `islands/lib/mount.tsx:36-46` writes a failed `app.connect()` into `.island-waiting-sub`, so the standalone smoke can't tell "no host" from "broken bridge". Distinguish the two (timeout vs. rejected handshake) in the message and expose it to the smoke.
- [ ] **Per-scene dense contact sheets for `refine-scene`.** Today it sees the recording's director sheets; Studio plans a dense per-scene sheet (`apps/studio/src/lib/contactSheet.ts:152`, `planSceneContactSheet`). `scripts/sheet-plan.ts` does global planning only — port the per-scene planner and feed `refine-scene` from it.
- [x] closed — keep `blog-editor`'s cross-reference **`styles.css` import order is load-bearing** (`source(none)` before `studio/index.css`) in both `islands/cut-editor/styles.css:11-31` and `islands/blog-editor/styles.css:1-13`, but only `cut-editor` carries the explanatory comment block. Copy it to `blog-editor`, and note that a second Studio component rendered in an island needs its own `@source`.

Verify: `pnpm workflow-studio:lint && pnpm workflow-studio:stage && pnpm workflow-studio:build && pnpm workflow-studio:test && pnpm workflow-studio:typecheck`.

