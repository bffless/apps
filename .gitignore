# dependencies
node_modules
# pnpm's project-local store — created inside the Sandcastle sandbox (the worktree
# sits on a different filesystem than the agent's home, so pnpm puts its store here).
.pnpm-store/

# builds
dist
*.tsbuildinfo
.node_modules

# test / coverage artifacts
coverage
playwright-report
test-results
screenshots

# local scratch
*.local
.studio-test.mp4
.bffless-backups/
.superpowers/

# editor / OS
.DS_Store
.claude/settings.local.json

# root MCP config — generated inside the Sandcastle sandbox from
# .sandcastle/mcp.json; never commit it (would auto-load into a cloner's Claude
# Code against the maintainers' instance — see #96).
.mcp.json
