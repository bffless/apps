function handler({ request, deployment }) { var body = (request && request.body) || {}; var path = String(body.path || ''); var entry = String(body.entry || 'index.html'); var owner = (deployment && deployment.owner) || ''; var repo = (deployment && deployment.repo) || ''; var storagePrefix = (path && owner && repo) ? (owner + '/' + repo + '/uploads/content/' + path) : ''; var url = path ? ('/api/uploads/content/' + path + '/' + entry) : null; return { path: path, entry: entry, storagePrefix: storagePrefix, url: url }; }