function handler({ steps }) { var pre=steps.pre||{}; var f=steps.folder||null; var isFolder=!!f&&f.nodeType==='folder'; var isOwner=!!pre.uid&&!!f&&f.ownerId===pre.uid; var bad=(pre.valid!==true)||!isFolder; var allowed=!bad&&(pre.isAdmin===true||isOwner); return { allowed: allowed, badRequest: bad, denied: !allowed&&!bad, saveMode: allowed&&pre.hasMode===true, saveFeedExcluded: allowed&&pre.hasFeedExcluded===true }; }