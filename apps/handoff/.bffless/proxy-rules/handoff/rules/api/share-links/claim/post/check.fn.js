function handler({ steps, utils }) { var l = (steps && steps.link) || {}; if (l == null || typeof l !== 'object') l = {}; var folderId = l.folderId || null; var revoked = l.revoked === true || l.revoked === 'true'; var exp = (l.expiresMs != null) ? Number(l.expiresMs) : null; var expired = (exp != null && !isNaN(exp)) ? (Date.now() > exp) : false; var valid = !!folderId && !revoked && !expired; var setCookie = ''; if (valid) { var payload = utils.base64urlEncode(JSON.stringify({ s: String(folderId), exp: Date.now() + 1800000 })); setCookie = 'hf_s=' + payload + '.' + utils.sign(payload) + '; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=1800'; } return { valid: valid, invalid: !valid, folderId: valid ? folderId : null, setCookie: setCookie }; }