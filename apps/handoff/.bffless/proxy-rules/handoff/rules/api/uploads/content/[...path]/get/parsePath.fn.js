function handler({ request, deployment }) { var p = (request && request.path) || ''; var marker = '/api/uploads/content/'; var i = p.indexOf(marker); var rest = (i >= 0) ? p.slice(i + marker.length) : ''; var q = rest.indexOf('?'); if (q >= 0) rest = rest.slice(0, q); var segs = rest.split('/'); for (var s = 0; s < segs.length; s++) { try { segs[s] = decodeURIComponent(segs[s]); } catch (e) { /* malformed escape - keep raw */ } } rest = segs.join('/'); var owner = (deployment && deployment.owner) || ''; var repo = (deployment && deployment.repo) || ''; var fullKey = (rest && owner && repo) ? (owner + '/' + repo + '/uploads/content/' + rest) : ''; return { rest: rest, fullKey: fullKey, hasKey: !!fullKey }; }