# GENERATED by scripts/build-mcp.mjs from src/mcp/mcpConfig.ts — do not edit; run `pnpm --filter workflow mcp:build`.
targetUrl: pipeline
order: 41
pipeline:
  name: MCP step view resource
  description: "The step view (ui://bffless/workflow/step-view.4f8f3132.html, spec 10; apps#587): /step.html fetched in-process from the harness bundle and answered as text/html, the way resources/read serves it."
  steps:
    - id: route
      name: route
      handler: function_handler
      code: ../../../../../../mcp-fn/route.fn.js
    - id: stepView
      name: stepView
      handler: http_request
      config:
        condition: steps.route.isStepView
        url: steps.route.stepViewUrl
        method: GET
        headers:
          x-original-uri: steps.route.stepViewPath
          x-forwarded-host: steps.route.host
        forwardAuth: true
        failOnError: false
    - id: respond
      name: respond
      handler: response_handler
      config:
        body: "{{{steps.stepView.body}}}"
        status: 200
        headers:
          Cache-Control: no-store
        contentType: text/html; charset=utf-8
  validators:
    - type: auth_required
      config:
        allowApiKey: true
        requiredScopes:
          - workflow:read
description: The step view as a ui:// resource — scope workflow:read.
