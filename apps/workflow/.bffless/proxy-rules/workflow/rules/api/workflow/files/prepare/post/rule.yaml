targetUrl: pipeline
order: 20
pipeline:
  name: Prepare workflow file upload
  description: "Mint a presigned PUT under workflows/<impl>/<workflow>/<scope>/ (D7/D18). Per-input accept/maxSize are enforced client-side in M1; the static 5 GB cap is the server backstop (the Studio port's recordings input allows up to 5GB, and the bucket PUT is direct so this only bounds the register-side sanity check)."
  steps:
    - id: prepare
      name: prepare
      handler: presigned_upload
      config:
        subDir: workflows/{{request.body.impl}}/{{request.body.workflow}}/{{request.body.scope}}
        filename: request.body.filename
        expiresIn: 3600
        maxFileSize: 5368709120
    - id: respond
      name: respond
      handler: response_handler
      config:
        body: "{{{steps.prepare}}}"
        status: 200
        contentType: application/json
  validators:
    - type: auth_required
      config: { allowApiKey: true }
description: "Files trio 1/3: presigned PUT for kickoff/form uploads and registered pipeline outputs."
