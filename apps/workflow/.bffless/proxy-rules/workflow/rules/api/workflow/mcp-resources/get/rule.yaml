# GENERATED by scripts/build-mcp.mjs from src/mcp/mcpConfig.ts — do not edit; run `pnpm --filter workflow mcp:build`.
targetUrl: pipeline
order: 41
pipeline:
  name: MCP resources list
  description: "What the harness enumerates for resources/list: every island of every published implementation (spec 10). CE's mcp_handler adds the step view (a static resource of the endpoint config) and every resource's generated _meta.ui CSP."
  steps:
    - id: route
      name: route
      handler: function_handler
      code: ../../../../../mcp-fn/route.fn.js
    - id: aliases
      name: aliases
      handler: http_request
      config:
        condition: steps.route.isAliases
        url: steps.route.aliasesUrl
        method: GET
        forwardAuth: true
        failOnError: false
    - id: plan
      name: plan
      handler: function_handler
      code: ../../../../../mcp-fn/plan.fn.js
    - id: index1
      name: index1
      handler: http_request
      config:
        condition: steps.plan.has1
        url: steps.plan.url1
        method: GET
        headers:
          x-original-uri: steps.plan.path1
          x-forwarded-host: steps.route.host
        forwardAuth: true
        failOnError: false
    - id: index2
      name: index2
      handler: http_request
      config:
        condition: steps.plan.has2
        url: steps.plan.url2
        method: GET
        headers:
          x-original-uri: steps.plan.path2
          x-forwarded-host: steps.route.host
        forwardAuth: true
        failOnError: false
    - id: index3
      name: index3
      handler: http_request
      config:
        condition: steps.plan.has3
        url: steps.plan.url3
        method: GET
        headers:
          x-original-uri: steps.plan.path3
          x-forwarded-host: steps.route.host
        forwardAuth: true
        failOnError: false
    - id: reply
      name: reply
      handler: function_handler
      code: ../../../../../mcp-fn/reply.fn.js
    - id: respond
      name: respond
      handler: response_handler
      config:
        body: "{{{steps.reply.json}}}"
        status: 200
        headers:
          Cache-Control: no-store
        contentType: application/json
  validators:
    - type: auth_required
      config:
        allowApiKey: true
        requiredScopes:
          - workflow:read
description: The MCP resources list — scope workflow:read.
