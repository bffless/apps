# GENERATED by scripts/build-mcp.mjs from src/mcp/mcpConfig.ts — do not edit; run `pnpm --filter workflow mcp:build`.
targetUrl: pipeline
order: 40
pipeline:
  name: MCP tool workflow.start
  description: "One catalog tool as one rule (spec 10, GA shape): CE's mcp_handler invokes it in-process as the caller; the body is the tool's arguments, the answer is a catalog CallToolResult. The validator is where the tool's scope is enforced (D23). Functions are the shared bundles under mcp-fn/ (route → the flags this rule's static steps read; reply → the result)."
  steps:
    - id: route
      name: route
      handler: function_handler
      code: ../../../../../../mcp-fn/route.fn.js
    - id: index
      name: index
      handler: http_request
      config:
        condition: steps.route.needsIndex
        url: steps.route.indexUrl
        method: GET
        headers:
          x-original-uri: steps.route.indexPath
          x-forwarded-host: steps.route.host
        forwardAuth: true
        failOnError: false
    - id: plan
      name: plan
      handler: function_handler
      code: ../../../../../../mcp-fn/plan.fn.js
    - id: drive
      name: drive
      handler: http_request
      config:
        condition: steps.plan.isDrive
        url: steps.plan.driveUrl
        method: POST
        headers:
          x-original-uri: steps.plan.drivePath
          x-forwarded-host: steps.route.host
        forwardAuth: true
        failOnError: false
        body: steps.plan.driveBody
    - id: reply
      name: reply
      handler: function_handler
      code: ../../../../../../mcp-fn/reply.fn.js
    - id: respond
      name: respond
      handler: response_handler
      config:
        body: "{{{steps.reply.json}}}"
        status: 200
        headers:
          Cache-Control: no-store
        contentType: application/json
  validators:
    - type: auth_required
      config:
        allowApiKey: true
        requiredScopes:
          - workflow:run
description: workflow.start over the MCP endpoint — scope workflow:run.
