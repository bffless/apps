# GENERATED by scripts/build-mcp.mjs from src/mcp/mcpConfig.ts — do not edit; run `pnpm --filter workflow mcp:build`.
targetUrl: pipeline
order: 40
pipeline:
  name: MCP tool workflow.stepView
  description: "One catalog tool as one rule (spec 10, GA shape): CE's mcp_handler invokes it in-process as the caller; the body is the tool's arguments, the answer is a catalog CallToolResult. The validator is where the tool's scope is enforced (D23). Functions are the shared bundles under mcp-fn/ (route → the flags this rule's static steps read; reply → the result)."
  steps:
    - id: route
      name: route
      handler: function_handler
      code: ../../../../../../mcp-fn/route.fn.js
    - id: run
      name: run
      handler: data_query
      config:
        condition: steps.route.needsRun
        schemaId: $schema:workflow_runs
        limit: 1
        filters:
          runId:
            op: eq
            value: steps.route.runId
    - id: steps
      name: steps
      handler: data_query
      config:
        condition: steps.route.needsRun
        schemaId: $schema:workflow_run_steps
        limit: 1000
        filters:
          runId:
            op: eq
            value: steps.route.runId
    - id: plan
      name: plan
      handler: function_handler
      code: ../../../../../../mcp-fn/plan.fn.js
    - id: island
      name: island
      handler: http_request
      config:
        condition: steps.plan.hasIsland
        url: steps.plan.islandUrl
        method: GET
        headers:
          x-original-uri: steps.plan.islandPath
          x-forwarded-host: steps.route.host
        forwardAuth: true
        failOnError: false
    - id: reply
      name: reply
      handler: function_handler
      code: ../../../../../../mcp-fn/reply.fn.js
    - id: respond
      name: respond
      handler: response_handler
      config:
        body: "{{{steps.reply.json}}}"
        status: 200
        headers:
          Cache-Control: no-store
        contentType: application/json
  validators:
    - type: auth_required
      config:
        allowApiKey: true
        requiredScopes:
          - workflow:read
description: workflow.stepView over the MCP endpoint — scope workflow:read.
