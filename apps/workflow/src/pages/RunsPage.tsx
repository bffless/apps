/** Past runs of one workflow (08). Stub — the table lands with the runs task. */
export function RunsPage() {
  return (
    <section className="page">
      <h1 className="page-title">Past runs</h1>
      <p className="note">The past-runs table is not built yet.</p>
    </section>
  )
}
