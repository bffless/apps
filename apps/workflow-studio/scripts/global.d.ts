/**
 * Scaffolding only. Task 22 adds the first real `script`-step module under `scripts/`;
 * until then this is the directory's only file, so `tsc -p tsconfig.scripts.json` has at
 * least one input to check (TypeScript errors "No inputs were found" on an include glob
 * that matches zero files). Safe to delete once a real script exists — `tsconfig.scripts.json`
 * already includes all of `scripts/**`.
 */
export {}
