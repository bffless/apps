function handler({ request }) {
  // Scaffolded by `workflow add` — replace with __SEGMENT__'s real behavior.
  // `request.body` is the POST body; this stub echoes it back unchanged.
  return { ok: true, body: request.body ?? null }
}
