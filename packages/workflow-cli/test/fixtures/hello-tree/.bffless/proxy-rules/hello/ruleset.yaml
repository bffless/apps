name: hello
description: "The `hello` test implementation's backend: echo, slow job + poll, fail-on-purpose, analyze (M2) — the pipelines docs/spec/examples/hello.workflow.yaml calls. Decoys planted for the rename engine's tests: othello, shellhello — neither should ever be rewritten. Spec: apps/workflow/docs/spec/06."
